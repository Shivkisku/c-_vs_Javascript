#include <iostream>
#include <string>

int main() {
    int number = 5 + 2;

    // Convert the number to a string and concatenate with " Hello"
    std::cout << std::to_string(number) + " Hello" << std::endl;

    return 0;
}
